package com.apiguard.server.repo;

import com.apiguard.server.domain.FieldSourceEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FieldSourceRepository extends JpaRepository<FieldSourceEntity, Long> {
}
